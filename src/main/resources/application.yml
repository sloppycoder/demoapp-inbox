spring:
  application:
    name: inbox
  data:
    mongodb:
      uri: mongodb://dev:dev@localhost:27017/dev?authSource=dev

logging.level:
  org.springframework.data.mongodb.core.ReactiveMongoTemplate: debug
